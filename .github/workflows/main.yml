name: This is my first workflow in github actions
on: workflow_dispatch
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: Execute a linux command 
      run: echo "Hello All"
    - name: Execute command2
      run: hostname -s
